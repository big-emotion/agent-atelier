{
  "name": "@big-emotion/agent-atelier",
  "version": "0.2.0",
  "private": true,
  "license": "MIT",
  "description": "Agent Atelier: Big Emotion's curated, open-source collection of Claude Code skills, commands, and agents — packaged as a multi-plugin marketplace.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test scripts/*.test.mjs",
    "check:templates": "node scripts/check-templates.mjs",
    "check:manifests": "node scripts/check-manifests.mjs",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2"
  }
}
